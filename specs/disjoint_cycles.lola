input x : Int
input y : Int

output a := x + c.offset(by: -1).defaults(to: 0)
output b := a + 1
output c := b + 1

output d := x + f.offset(by: -2).defaults(to: 0)
output e := d + 2
output f := e + 2

output g := (y * 10) / 2 + i.offset(by: -1).defaults(to: 0) 
output h := g * 10
output i := h + 1