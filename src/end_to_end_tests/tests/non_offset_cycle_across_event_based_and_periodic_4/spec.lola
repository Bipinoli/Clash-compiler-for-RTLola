input x: Int 

output a := x + 1
output b @1Hz := a.hold(or: 0) + e.hold(or: 1)
output c := x + b.hold(or: 0) + f.hold(or: 1) 
output d @0.5Hz := c.hold(or: 3) + 1
output e := d.hold(or: 0) + a
output f @0.25Hz := b.hold(or: 10)  
