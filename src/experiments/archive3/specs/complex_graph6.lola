output x @1kHz := x.offset(by: -1).defaults(to: 0) + 1
output a := x + 1
output b := x + f.offset(by: -10).defaults(to: 0)
output c := a + b
output d := c + 1
output e := d + 1
output f @1kHz := e.aggregate(over: 1s, using: sum) + b