input x : Int

output a := x.offset(by: -1).defaults(to: 0) + 10 
output b := a * 10 + x
output c @1kHz := b.aggregate(over: 0.003s, using: sum)
output d @2kHz := a.aggregate(over: 0.003s, using: sum)
output e @1kHz := c.aggregate(over: 0.003s, using: sum)